# Book covers

Drop cover images here (e.g. `chromatrix.jpg`, roughly 2:3 portrait) and point a
manifest entry at them with `cover: "covers/chromatrix.jpg"`.

Missing covers never break the page — the switcher and book card fall back to a
lettered tile using the book title's first letter.
